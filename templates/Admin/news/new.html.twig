{% extends 'base.html.twig' %}

{% block title %}Création d'une nouvelle news{% endblock %}

{% block body %}
    <div class="d-flex">
        <a href="{{ path('admin_news_index') }}">
            <img src="{{ asset('images/return.png') }}" class="m-2 p-2" width="70" height="60" alt="">
        </a>
        <div class="h1 m-auto"><img src="{{ asset('images/svg/create.png') }}" alt="Création d'un élément"> News</div>
    </div>

    {{ include('Admin/news/_form.html.twig') }}

{% endblock %}
