{% extends 'base.html.twig' %}

{% block title %}Édition de la News "{{  news.title }}"{% endblock %}

{% block body %}
    <div class="d-flex">
        <a href="{{ path('admin_news_index') }}">
            <img src="{{ asset('images/return.png') }}" class="m-2 p-2" width="70" height="60" alt="">
        </a>
        <div class="h1 m-auto">Édition de la News "{{  news.title }}"</div>
    </div>

    {{ include('Admin/news/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('Admin/news/_delete_form.html.twig') }}
{% endblock %}
