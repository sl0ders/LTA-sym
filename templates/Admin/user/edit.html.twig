{% extends 'base.html.twig' %}

{% block title %}Édition de l'utilisateur {{ user.name }}{% endblock %}

{% block body %}
    <h1>Édition de l'utilisateur {{ user.name }}</h1>

    {{ include('Admin/user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_user_index') }}"><img src="{{ asset('images/svg/return.png') }}" alt="">Retouner au menu précédent</a>

    {{ include('Admin/user/_delete_form.html.twig') }}
{% endblock %}
