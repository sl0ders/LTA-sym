{% extends 'base.html.twig' %}

{% block title %}Édition de la commande n°{{ order.nCmd }}{% endblock %}

{% block body %}
    <h1>Édition de la commande n°{{ order.nCmd }}</h1>

    {{ include('Admin/orders/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_orders_index') }}"><img src="{{ asset('images/svg/return.png') }}" alt="">Retouner au menu précédent</a>

    {{ include('Admin/orders/_delete_form.html.twig') }}
{% endblock %}
